spring:
  application:
    name: web-client
  thymeleaf:
    prefix: classpath:/static/
    suffix: .html
server:
  port: 8080
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8000/eureka/